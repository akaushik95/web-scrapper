# make connection using mongo or mysql
# Just keep in mind that only one connection pool should be made to one database cluster.
# We can parse all dependencies and store it in a single variable. These should not be initialised again and again.

